<!DOCTYPE html>
<html>
  <head>
    <title>Turbo Chatroom Admin</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <header class="p-3 bg-dark text-white">
    <div class="container">
      <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
          <% if current_user %>
            <li><%= link_to "Messages", messages_index_path, class: "nav-link px-2 text-white"  %></li>
          <% end %>
        </ul>

        <div class="text-end">
          <%= link_to "Sign in", new_user_session_path, class: "btn btn-outline-light me-2" if !current_user %>
          <%= button_to "Logout", destroy_user_session_path, method: :delete, class:'btn btn-danger', form_class: 'd-inline' if current_user %>
        </div>
      </div>
    </div>
  </header>

  <main class="flex-shrink-0">
    <div class="container">
      <% if notice %>
        <div class="alert alert-primary mt-3" role="alert">
          <%= notice %>
        </div>
      <% end %>
      <% if alert %>
      <div class="alert alert-danger mt-3" role="alert">
        <%= alert %>
      </div>
      <% end %>

      <%= yield %>
    </div>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
          integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </body>
</html>
